#ifndef _PROCESS_H_
#define _PROCESS_H_

/* proxy_request
 * 	Do what ever the proxie do
 * Params
 * 	lsock	local socket 
 * 	rsock	remote socket (server's socket)
 * 	data	extra data
 * Return 
 * 	TODO return
 */
int
proxy_request ( struct opt *opt );


#endif
